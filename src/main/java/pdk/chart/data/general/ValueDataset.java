package pdk.chart.data.general;

import pdk.chart.data.Value;

/**
 * An interface for a dataset that returns a single value.
 */
public interface ValueDataset extends Value, Dataset {

    // no additional methods required

}
